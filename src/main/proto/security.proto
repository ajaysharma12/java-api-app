syntax = "proto2";

package example.security;

option java_package = "in.ajsd.example.security";

message Session {
  required string session_id = 1;
  required string current_user_public_id = 2;
  // required string session_secret = 3;
  optional bool is_secure = 4 [default = false];
  optional bool is_active = 5 [default = false];
}
